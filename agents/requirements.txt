snowflake-connector-python>=3.6.0
cryptography>=41.0.0
pyyaml>=6.0.1
jinja2>=3.1.0
rich>=13.7.0          # optional — pretty terminal output, falls back gracefully
anthropic>=0.25.0     # used by description / test / lineage agents (Post 2)
